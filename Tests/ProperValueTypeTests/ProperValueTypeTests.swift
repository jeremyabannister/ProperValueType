    import XCTest
    @testable import ProperValueType

    final class ProperValueTypeTests: XCTestCase {
        func testExample() {
            // This is an example of a functional test case.
            // Use XCTAssert and related functions to verify your tests produce the correct
            // results.
            XCTAssertEqual(ProperValueType().text, "Hello, World!")
        }
    }
